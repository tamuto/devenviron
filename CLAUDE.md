# CLAUDE.md

DevEnvironで作業する際の方針を記載する。

このファイルには**方針と禁止事項のみ**を書く。
プロジェクトの説明・手順・コマンド一覧といった事実の記述は陳腐化するため、
[README.md](./README.md) と [docs/](./docs/) に集約し、ここには置かない。
未対応の改善課題は [TODO.md](./TODO.md) を参照。

## 採用しない方針

以下はいずれも検討したうえで採用していない。提案・実装しないこと。

### CIによるイメージビルド

Jetson(L4T)系のイメージは実機でのビルドが前提であり、CI環境では再現できない。
x86系だけをCI化するとビルド経路が二系統に分かれ、
「どのイメージがどこで焼かれたか」が不明瞭になって管理が破綻する。
イメージのビルドはローカル実行に統一する。

※ shellcheck等、ビルドを伴わない静的チェックはこの方針の対象外。

### ソフトウェアのバージョン固定

開発環境であり、常に最新のツールチェインを使えることを優先する。
`constraints.txt` やバージョンpinは導入しない。
代わりに、ビルド時点の構成を `manifests/` に記録することで追跡可能性を担保している。

### `latest` タグ

マルチアーキ構成のため、別アーキのイメージを上書きする事故が起きる。
後述のタグ不変ポリシーとも本質的に相容れないため、付与もpushもしない。

## イメージの系統とバージョン体系

2系統を併走させる。取り違えないこと。

| 系統 | イメージ名 | バージョン体系 | 用途 |
| --- | --- | --- | --- |
| 既存 | `tamuto/devenviron` | SemVer（例 `0.41.0`） | VSCodeのdevcontainerを前提とした開発環境 |
| 新規 | `tamuto/denv-cc-remote` | CalVer（例 `2026.08.0`） | Claude Codeをremote-controlで動かす環境 |

- 新系統は**devcontainerを前提としない**。必要に応じてcomposeを用いる。
  セットアップ手順も既存とは別系統になるため、既存の`setup.sh`を流用しない。
- CalVerは`YYYY.MM.<patch>`形式。同一月内の再ビルドは`<patch>`で区別する。
  「常に最新を導入する」方針では中身が実質ビルド時期で決まるため、
  番号自体が「いつ時点の構成か」を表す体系を採る。
- 後述のタグ不変ポリシーは両系統に等しく適用される。

## 禁止事項

### 一度pushしたイメージタグは上書きしない

メンバー間の環境統一は「同じタグ = 同じイメージ」で担保している。
中身を変える場合は必ず新しいタグを発行する。

### `docker build` と `deploy.sh` は実行しない

レジストリや配布物へ影響が出る操作であり、
かつ特殊環境(Jetson/L4T)は対応する実機がないとビルドできない。
実行はユーザが行う。スクリプトの修正までに留めること。

## 変更時に連動して必要な作業

- イメージをビルドしたら `manifests/devenviron-<image-tag>.txt` を必ずコミットする。
  バージョン固定を行わない方針のため、この記録が実質的な唯一の変更履歴となる。
- `template/container/Dockerfile.tmpl` の変更は `scripts/build_*.sh` の全ターゲットに波及する。
  Jetson(L4T)系を含むため、x86で通ったことをもって問題なしと判断しない。

## 記述言語

ドキュメント・コミットメッセージ・コード内コメントは日本語で記述する。
